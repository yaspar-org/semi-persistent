;; EXPECT: ok
;; EVAL: both
;; Declared AC laws are enforced while terms are constructed.

;; ANCHOR: ac-canonization
(sort Math)
(function a () Math)
(function b () Math)
(function c () Math)
(function Add (Math) Math :assoc-comm)

(let nested (Add a (Add b c)))
(check (= nested (Add c a b)))
;; ANCHOR_END: ac-canonization
