# Ignore the `build/` directory in the package root.
/build
.DS_Store
target/

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.pdf

# Kiro
.kiro/
session*.json
session*.md

# Claude Code: agent state, transcripts and per-tool config, at any depth.
# The repo-root .claude/ was previously carved out as a committed sdd harness;
# nothing was ever tracked there, the directory is empty, and the document that
# described it is gone, so the exception went with it.
.claude/
CLAUDE.md

# sdd run evidence (ephemeral; distilled into <crate>/doc/verification/)
runs/

# bare `verus` invocations emit this into cwd
liblib.rlib

# Verus SMT solver traces (local debugging artifact, machine-specific)
.verus-solver-log/

# Regenerated by every benchmark run; the committed CSVs are the artifacts.
*.stats.json
