# Ignore the `build/` directory in the package root.
/build
.DS_Store
target/

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.pdf

# Kiro
.kiro/
session*.json
session*.md

# Claude Code (agent state / transcripts — never commit),
# EXCEPT the repo-root .claude/ — that is the committed sdd harness
# (agents/skills/workflows/probes + CALIBRATION.md), not agent state.
**/.claude/
!/.claude/
CLAUDE.md

# sdd run evidence (ephemeral; distilled into <crate>/doc/verification/)
runs/
